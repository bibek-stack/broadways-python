# mutuable an immutable objects

# if an objects once created can be change d later then the objects is an mutable objects

# but if an objects once created can never be changed throughout its life cycle then it is an immutable objects

# list , dictionary, set are the eample of mutuable object Numbers 
# Tuple, Boolean are the example immutable object Numbers


a =[1,2,3,]
a[1] = 5
a

b = (1,2,3)
b[1] = 5 # it raises error because tuple is immutable